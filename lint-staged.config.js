module.exports = {
  // TypeScript and JavaScript files
  '*.{ts,tsx,js,jsx}': ['prettier --write'],

  // JSON files
  '*.json': ['prettier --write'],

  // CSS files
  '*.css': ['prettier --write'],

  // Markdown files
  '*.md': ['prettier --write'],
}
